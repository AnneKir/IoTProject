import { IBiFunction } from '../..';
import { IFunction } from '../..';
export declare class BiFunction<T, U, R> implements IBiFunction<T, U, R> {
    private _function;
    getNativeFunction(): any;
    constructor(f: (t: T, u: U) => R);
    andThen<V>(after: IFunction<R, V>): IBiFunction<T, U, R>;
    apply(t: T, u: U): R;
}
