import { IMapEntry } from '../../../..';
export declare class MapEntry<K, V> implements IMapEntry<K, V> {
    private _key;
    private _value;
    constructor(key: K, value: V);
    equals(o: any): boolean;
    getKey(): K;
    getValue(): V;
    hashCode(): number;
    setValue(value: V): V;
}
