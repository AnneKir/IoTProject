import { ISet } from '../..';
export declare class Set<E> implements ISet<E> {
    private _elements;
    constructor();
    add(e: E): boolean;
    addAll(c: E[]): boolean;
    clear(): void;
    contains(o: any): boolean;
    containsAll(c: any[]): boolean;
    equals(o: ISet<E>): boolean;
    hashCode(): number;
    isEmpty(): boolean;
    remove(o: any): boolean;
    removeAll(c: any[]): boolean;
    retainAll(c: any[]): boolean;
    size(): number;
    toArray(): E[];
}
