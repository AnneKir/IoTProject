import { Node } from '../../..';
import { ILinkedList } from '../../..';
export declare class DoublyLinkedList<T> implements ILinkedList<T> {
    private _head?;
    private _tail?;
    private _size;
    constructor();
    length(): number;
    empty(): boolean;
    unshift(item: T): number;
    shift(): T;
    push(item: T): void;
    pop(): T;
    remove(item: T): T;
    forEach(callback: Function): void;
    filter(callback: Function): DoublyLinkedList<T>;
    includes(item: T): boolean;
    indexOf(item: T): number;
    private create(item);
    peek(): T;
    back(): T;
    clear(): void;
    contains(item: T): boolean;
    find(item: T): Node<T>;
    toArray(): T[];
}
