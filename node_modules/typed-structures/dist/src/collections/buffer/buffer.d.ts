import { IBuffer } from '../..';
export declare abstract class Buffer implements IBuffer {
    private readonly _cap;
    private _limit;
    private _pos;
    private _mark;
    protected constructor(capacity: number, limit: number, position: number, mark: number);
    capacity(): number;
    clear(): IBuffer;
    flip(): IBuffer;
    hasRemaining(): boolean;
    abstract isReadOnly(): boolean;
    limit(): number;
    limit(newLimit: number): IBuffer;
    mark(): IBuffer;
    position(): number;
    position(newPosition: number): IBuffer;
    remaining(): number;
    reset(): IBuffer;
    rewind(): IBuffer;
}
