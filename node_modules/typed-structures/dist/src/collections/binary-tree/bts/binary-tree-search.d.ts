import { IBinaryTree } from '../bt-interface';
import { BNode } from '../utils/node/b-node';
export declare class BinaryTreeSearch<T> implements IBinaryTree<T> {
    private _root?;
    constructor();
    root(): BNode<T>;
    add(item: T): boolean;
    remove(item: T): boolean;
    find(root: BNode<T>, item: T): T;
    private create(item);
}
