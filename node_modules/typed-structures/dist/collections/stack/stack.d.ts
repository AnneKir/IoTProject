import { IStack } from '../..';
export declare class Stack<T> implements IStack<T> {
    private _items;
    constructor();
    length(): number;
    stack(item: T): void;
    unstack(): T;
    peek(): T;
    empty(): boolean;
    toArray(): T[];
}
