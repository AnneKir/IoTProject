import { Node } from '../../..';
import { ILinkedList } from '../../..';
export declare class SinglyLinkedList<T> implements ILinkedList<T> {
    private _head?;
    private _size;
    constructor();
    clear(): void;
    contains(item: T): boolean;
    empty(): boolean;
    filter(callback: Function): SinglyLinkedList<T>;
    find(item: T): Node<T>;
    forEach(callback: Function): void;
    includes(item: T): boolean;
    indexOf(item: T): number;
    length(): number;
    peek(): T;
    back(): T;
    pop(): T;
    push(item: T): void;
    remove(item: T): T;
    shift(): T;
    unshift(item: T): number;
    toArray(): T[];
    private create(item);
}
