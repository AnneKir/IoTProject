import { INode } from '../../../..';
export declare class Node<T> implements INode {
    private _data;
    private _next?;
    private _previous?;
    constructor(data: T);
    data: T;
    next: Node<T> | undefined;
    previous: Node<T> | undefined;
    hasNext(): boolean;
    hasPrevious(): boolean;
}
