import { Buffer } from '../../..';
import { IBuffer } from '../../..';
export declare class GenericRingBuffer<T> extends Buffer {
    private readonly _buf;
    private readonly _readonly;
    private _readPosition;
    constructor(capacity: number, limit: number, position: number, readPosition: number, mark: number, readonly?: boolean);
    get(): T;
    put(element: T): IBuffer;
    isReadOnly(): boolean;
    readPosition(): number;
    readPosition(newPosition: number): IBuffer;
    toArray(): T[];
}
