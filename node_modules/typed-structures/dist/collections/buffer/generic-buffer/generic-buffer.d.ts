import { Buffer } from '../../..';
import { IBuffer } from '../../..';
export declare class GenericBuffer<T> extends Buffer {
    private readonly _buf;
    private readonly _readonly;
    constructor(capacity: number, limit: number, position: number, mark: number, readonly?: boolean);
    get(): T;
    put(element: T): IBuffer;
    isReadOnly(): boolean;
    toArray(): T[];
}
