import React, { ReactElement } from 'react';
import { ReactSimplePeerModel } from '../index';
interface PeersProps {
    children: (connections: ReactSimplePeerModel[]) => void | ReactElement | ReactElement[];
}
interface PeersState {
    connections: ReactSimplePeerModel[];
}
export declare class Peers extends React.Component<PeersProps, PeersState> {
    readonly state: {
        connections: never[];
    };
    componentDidMount(): void;
    render(): React.ReactElement<any, string | ((props: any) => React.ReactElement<any, string | any | (new (props: any) => React.Component<any, any, any>)> | null) | (new (props: any) => React.Component<any, any, any>)>[] | JSX.Element;
}
export {};
